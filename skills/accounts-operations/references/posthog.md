# PostHog

Research the [PostHog API documentation](https://posthog.com/docs/api) and the connected deployment's API version before every new request.

Verify cloud or self-hosted host, organization, project, and environment scope; use the current query API for analytical queries when supported. Check HogQL semantics, query lifecycle, response limits, pagination, rate limits, retention, and destructive-operation behavior.
