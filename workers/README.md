# workers

Notion worker code lives here when an agent needs executable logic.

Keep reusable API operations in their owning repo. Accounts Ops provider work belongs in `$HOME/Dev/hellogafaro-accounts`.
